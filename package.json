{
  "name": "pinequest-s4-e1-team-8",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "dev:server": "bun run --cwd server dev",
    "dev:client": "bun run --cwd client dev",
    "test:agent": "bun test ./scripts/test-agent.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
