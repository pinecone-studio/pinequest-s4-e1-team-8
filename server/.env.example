# Copy to `.env.local` and fill in. Used by Drizzle Kit (`drizzle.config.ts`) for
# `drizzle-kit generate` against remote D1 over HTTP — not required for `wrangler dev` alone.

CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_DATABASE_ID=
CLOUDFLARE_D1_TOKEN=
